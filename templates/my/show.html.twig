{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
               <tr>
                <th>Image </th>
                <td> <img src="{{ product.Image}}" class="imgg"/> </td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.Name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ product.Category }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.Price }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('my_index') }}">back to list</a>

    <a href="{{ path('my_edit', {'id': product.id}) }}">edit</a>

    {{ include('my/_delete_form.html.twig') }}
{% endblock %}
